// src/routes/index.route.ts
import { Router } from 'express';
// Example imports for future routes:
// import userRoutes from './user.route'; 

const router = Router();

// Example route mounting:
// router.use('/users', userRoutes);

export default router;